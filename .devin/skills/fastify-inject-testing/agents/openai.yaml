interface:
  display_name: "Fastify Inject Testing"
  short_description: "Use when asked to test Fastify applications without network sockets: auth, validation errors, uploads, streams, plugins, hooks."
