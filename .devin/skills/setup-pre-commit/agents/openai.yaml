interface:
  display_name: "Setup Pre Commit"
  short_description: "Use when installing or repairing one repository-local pre-commit hook using the project's current gates, or when a repo needs package-manager-native commit-time checks."
