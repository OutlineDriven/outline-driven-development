interface:
  display_name: "Universal Invariant Baseline"
  short_description: "Use when a user explicitly invokes this skill to apply an invariant-first, fail-fast, special-case-eliminating baseline to a named implementation."
