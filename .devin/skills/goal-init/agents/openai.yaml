interface:
  display_name: "Goal Init"
  short_description: "Use when a durable effort needs an approved, checkable success predicate before work starts; writes the goal, verifier, budget, and run-log scaffold."
