interface:
  display_name: "Unleak Abstraction"
  short_description: "Use when an abstraction leak must be sealed as a module seam, configuration option, or explicit override, or exposed as a named boundary."
