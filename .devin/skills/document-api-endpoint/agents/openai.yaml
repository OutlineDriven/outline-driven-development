interface:
  display_name: "Document API Endpoint"
  short_description: "Use when reconciling an API endpoint's generated OpenAPI schema and declared response types with its actual runtime response."
