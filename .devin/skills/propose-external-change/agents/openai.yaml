interface:
  display_name: "Propose External Change"
  short_description: "Use when asked to change state in an external system, propose the write locally and halt at the human gate without executing it."
