interface:
  display_name: "Test Suite Acceleration"
  short_description: "Use when a test suite is too slow and needs acceleration without weakening behavior or coverage, or CI parallelization must fix serial execution."
