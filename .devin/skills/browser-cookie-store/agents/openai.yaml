interface:
  display_name: "Browser Cookie Store"
  short_description: "Use when the user runs /browser-cookie-store to populate the session cookie store from installed browsers for authenticated browsing."
