interface:
  display_name: "Claude Settings Audit"
  short_description: "Use when setting up a project, auditing agent command permissions, or asking which read-only bash commands and domains to allow."
