interface:
  display_name: "Prohibit Bad Habits"
  short_description: "Use when a user wants to define patterns the agent should not do; result is structured project docs or agent-rule files listing each prohibited pattern."
