interface:
  display_name: "Fastify Schema First Service"
  short_description: "Use when asked to build or extend a Fastify application: routes, plugins, JSON Schema validation/serialization, hooks/lifecycle, decorators, content types, and database wiring."
