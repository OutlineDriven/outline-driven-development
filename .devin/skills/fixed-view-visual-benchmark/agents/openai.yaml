interface:
  display_name: "Fixed View Visual Benchmark"
  short_description: "Use when a visual needs repeatable fixed-view rendering and independent rubric scoring: render the fixed view through a specified interface, score it against a frozen rubric, and prove the saved render clears the threshold."
