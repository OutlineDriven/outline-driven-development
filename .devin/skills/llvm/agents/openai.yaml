interface:
  display_name: "Llvm"
  short_description: "Use when working with LLVM IR as a user: emitting IR from clang, running opt passes, lowering with llc, reading IR, or finding a missed optimization."
