interface:
  display_name: "Port Static Analysis Rule"
  short_description: "Use when an existing static-analysis rule must be ported to another language or analyzer, producing an independently validated rule with vulnerable, safe, and edge-case parity tests."
