interface:
  display_name: "First Load Byte Reduction"
  short_description: "Use when a first screen needs lower transfer bytes without visual or behavioral change: measure compressed bytes, reduce assets and bundles to a budget, prove pixel identity, and pass the test suite."
