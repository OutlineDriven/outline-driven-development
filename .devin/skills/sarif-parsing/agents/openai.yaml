interface:
  display_name: "Sarif Parsing"
  short_description: "Use when a user supplies existing SARIF to inspect, filter, aggregate, deduplicate, diff, convert, or gate findings without running a scanner."
