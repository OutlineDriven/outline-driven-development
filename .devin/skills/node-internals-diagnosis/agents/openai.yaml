interface:
  display_name: "Node Internals Diagnosis"
  short_description: "Use when Node.js segfaults, addon crashes, leaks, event-loop anomalies, thread-pool saturation, V8 deoptimizations, or binding.gyp failures need diagnosis."
