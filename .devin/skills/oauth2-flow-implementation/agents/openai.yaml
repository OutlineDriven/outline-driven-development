interface:
  display_name: "Oauth2 Flow Implementation"
  short_description: "Use when asked to implement, debug, validate, or explain an OAuth 2.1 flow: auth code with PKCE, client credentials, device, or refresh."
