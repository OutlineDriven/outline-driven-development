interface:
  display_name: "Terraform Style Check"
  short_description: "Use when writing, reviewing, or generating Terraform HCL that must pass fmt and validate with correct file organization, naming, version pinning, and security defaults."
