interface:
  display_name: "Heaptrack"
  short_description: "Use when tracking heap allocations on Linux with heaptrack: allocation hotspots, peak heap, temporary allocations, leaks, or a diff of two runs via heaptrack_print."
