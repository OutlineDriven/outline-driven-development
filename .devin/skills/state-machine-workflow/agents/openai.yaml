interface:
  display_name: "State Machine Workflow"
  short_description: "Use when work has distinct modes and the user wants states, events, guards, outcomes, illegal transitions, not a prose todo list."
