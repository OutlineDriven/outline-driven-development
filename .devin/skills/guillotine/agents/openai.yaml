interface:
  display_name: "Guillotine"
  short_description: "Use when a repository or subsystem needs dead, duplicate, superseded, or generated residue removed across more than one artifact class — code, docs, tests, dependencies, config, workflows, generated files — while preserving observable behavior."
