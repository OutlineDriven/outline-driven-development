interface:
  display_name: "Orchestration Patterns"
  short_description: "Use when work decomposes across subagents or role panels: select a coupling-based orchestration mechanism, spawn bounded subagents, verify every artifact owner-side, and write only the reconciled synthesis."
