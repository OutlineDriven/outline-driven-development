interface:
  display_name: "Product Design"
  short_description: "Use when deciding what an interface should do before UI is built or audited: consequences, action scope, reachable states, naming, and accessibility."
