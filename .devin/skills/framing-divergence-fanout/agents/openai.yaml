interface:
  display_name: "Framing Divergence Fanout"
  short_description: "Use when a user suspects the current direction is tunnel-visioned or inherited its framing: spawn 2-5 independent reads, classify each, cluster by shared root, and surface divergent-incompatible reads first."
